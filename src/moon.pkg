import {
  "moonbitlang/core/debug",
  "moonbitlang/core/math",
  "tonyfettes/encoding",
  "moonbitlang/x/fs",
}

supported_targets = "native"

options(
  "is-main": true,
  link: { "native": { "stub-cc-flags": "${build.MAGICKWAND_STUB_CC_FLAGS}" } },
  "native-stub": [ "magick_stub.c" ],
)
